const { it } = require('node:test')
const isBook = require('../books')

it('Call all books', () =>{
  //stub
});

it('Retrieve a book by the OId', () =>{
  //stub
});

it('Add a new book to the collection', () =>{
  //stub
});

it('Update the book', () =>{
  //stub
});

it('Delete the book', () =>{
  //stub
});
